<%= form_for @comment do |f| %>

<%= f.label :comment %><br/>
<%= f.text_area :comment %><br/>

<%= f.hidden_field :date, value: DateTime.current.to_date %>
<%= f.hidden_field :user_id, session[:id] %>

<%= f.submit %>

<% end %>